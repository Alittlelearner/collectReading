# 项目待办

## 还未完成

### 1. 真正跑通 Web 端页面验收

- 在浏览器里完整手测首页、详情页、设置页、导入导出、擦亮流程
- 确认 `webDatabase` 对所有当前 SQL 分支都已覆盖
- 验证页面刷新后本地数据、擦亮配置、成就状态是否稳定持久化

### 2. 真机 / Expo 安装验收

- 用 Expo Go 或开发构建在手机上实际安装运行
- 检查导航、数据初始化、SQLite、通知权限、导入导出是否正常
- 确认 Android 与 iOS 至少有一端完成真机链路验证

### 3. MediaCrawler 接入结果需要再核实

- 当前仓库里没有看到明确完成接入的代码落点
- 如果目标仍是“仅用公开 API / 合法公开数据源”，需要重新界定 MediaCrawler 在本项目中的角色
- 明确哪些站点继续用公开 API，哪些站点保留通用解析兜底

### 4. 公开 API 覆盖范围还不完整

- 现在明确偏 API 优先的是 `Bilibili`、`Juejin`、`GitHub`
- 其余站点还没有统一整理成“公开 API 查询详情方式”清单
- 需要继续补全站点级能力矩阵：支持什么、字段完整度如何、失败后怎么兜底

### 5. 成就弹出反馈还不完整

- 当前成就状态会自动刷新
- 但“解锁后立即给用户明显提示”的体验链路还不完整
- 可以补成就 toast、弹窗或徽章动画

## 可优化项

### 1. 中文文案与编码统一

- 仓库里仍有不少中文乱码
- `app.json`、导航标题、部分页面和旧文案需要统一恢复为 UTF-8 正常文本
- 建议顺手建立统一文案表，减少散落硬编码

### 2. 详情页和列表页的阅读行为定义再统一

- 现在“打开原链接”会给单条收藏 `readCount + 1`
- “完成阅读”也会增加 `readCount`
- 需要进一步明确：`readCount` 表示打开次数、完成次数，还是总阅读行为次数

### 3. 擦亮与阅读统计口径再收敛

- `resurfaceCount`、`readCount`、`read_at`、`daily_stats.read_count` 已经能工作
- 但统计语义还可以更清晰
- 建议补一份字段口径说明，避免后续报表和成就规则混乱

### 4. 设置页体验优化

- 擦亮配置可用，但输入校验较弱
- 可以补最小值、非法输入提示、保存成功后的即时刷新
- 导入导出流程也可以增加更多异常提示

### 5. 解析器体系继续抽象

- 当前接口已经统一，但还能继续增强
- 可增加字段完整度评分、失败原因、站点能力声明、超时与重试策略
- 可把“API 优先 / HTML 兜底 / 通用兜底”做成显式策略

### 6. 解析结果缓存

- 同一链接重复解析时可以做缓存
- 减少重复请求，提高新增收藏速度
- 对公开 API 站点尤其有价值

### 7. 测试补齐

- 当前以手工检查和类型检查为主
- 建议补服务层测试，至少覆盖：
- `bookmarkService`
- `achievementService`
- `resurfaceService`
- `webDatabase`
- `ExtractorRegistry`

### 8. 统计和成就的自动回归验证

- 现在成就联动已补齐，但后续容易被新入口漏掉
- 可以增加一组最小业务回归测试：
- 新增收藏是否触发 `first_blood`
- 已读是否推进阅读类成就
- 新建笔记 / 标签是否推进对应成就
- 擦亮完成是否推进 `resurrector`

### 9. Web 与 Native 数据层继续收口

- 目前已经有统一入口，但 `webDatabase` 仍是手写 SQL 分支模拟
- 后续可以考虑进一步抽象 Repository 或 Query Adapter
- 降低两端逻辑不一致的风险

### 10. 项目文档继续补全

- 可以新增一份 `API_SUPPORT_MATRIX.md`
- 也可以新增一份 `DATA_MODEL.md`
- 如果后续继续做安装和预览验收，再补一份 `RUNBOOK.md`
