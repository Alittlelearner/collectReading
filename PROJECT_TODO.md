# 项目待办

## 高优先级

### 1. Web 端完整回归验收

- 覆盖首页、详情页、添加页、设置页、Wiki 页、我的书房。
- 验证左侧图标栏、侧边栏折叠、我的书房返回首页、添加页清空表单。
- 验证单条添加、批量添加、后台补全、标签编辑、收藏夹移动/复制。
- 验证停止服务再启动后 Web 数据是否仍可见。
- 验证 Web localStorage 防丢备份是否能从空 SQLite 自动恢复。

### 2. Native 真机验收

- Android 至少完成一次 Expo Go 或开发构建验证。
- iOS 如有条件再补验证。
- 重点检查 SQLite、通知权限、文件选择、分享、导入导出、打开链接。

### 3. 数据安全与备份体验

- 导出前显示最近备份时间。
- Web 端提供“下载当前自动备份”的独立入口。
- 导入完成后自动刷新首页、标签、收藏夹、统计和 Wiki 状态。
- 导入异常需要展示更细的失败原因。
- 支持导入前做 dry-run 检查，提示会新增/更新多少数据。

### 4. 自动化测试补齐

优先补服务层测试：

- `bookmarkService`
- `tagService`
- `folderService`
- `backupPayload`
- `resurfacePolicy`
- `achievementService`
- `ExtractorRegistry`

建议覆盖：

- 新增收藏去重
- 批量占位创建和后台补全
- 标签/收藏夹关系更新
- 软删除和恢复
- 导入导出完整字段
- 导入事务失败回滚
- 成就解锁条件

## 中优先级

### 1. 解析器能力矩阵

新增 `API_SUPPORT_MATRIX.md`，记录每个站点：

- 是否支持精准解析
- 是否需要 HTML
- 是否依赖公开 API
- 字段完整度
- 常见失败原因
- 兜底策略

### 2. 解析稳定性增强

- 解析失败原因结构化。
- 增加字段完整度评分。
- 对同一 URL 做短期缓存。
- 批量添加时显示后台补全进度。
- 失败的后台补全支持稍后重试。

### 3. 统计口径统一

需要明确并文档化：

- `readCount` 表示打开次数、完成次数，还是总阅读行为次数。
- `daily_stats.read_count` 是否只统计完成阅读。
- `resurfaceCount` 和通过擦亮完成阅读的关系。
- 已读归档与手动归档的区别。

### 4. 成就反馈体验

- 成就解锁后显示 toast 或弹窗。
- 我的书房展示最近解锁成就。
- 成就列表增加分类和进度。

### 5. 设置页体验增强

- 擦亮配置加数值校验。
- 重置数据前要求二次确认关键词。
- 导入导出增加加载状态，避免重复点击。
- Web 和 Native 的提示风格统一。

## 低优先级

### 1. 文案与视觉继续打磨

- 抽离统一文案表，减少散落硬编码。
- 检查长标题、长标签、长收藏夹名在 Web 和移动端的换行。
- 收藏详情页的标签和收藏夹弹窗可以继续统一视觉。

### 2. 代码结构继续收口

- 将备份导入导出 UI 逻辑从 `SettingsScreen` 再抽成 Hook 或 Service。
- 将 Web 侧边栏拆成独立组件。
- 将详情页标签选择器和收藏夹选择器抽成复用组件。
- 清理暂未使用的 `@react-navigation/bottom-tabs` 依赖。

### 3. 文档继续补齐

- `DATA_MODEL.md`：数据库表和字段口径。
- `RUNBOOK.md`：开发、验收、备份、恢复流程。
- `API_SUPPORT_MATRIX.md`：解析器能力矩阵。
- `RELEASE_NOTES.md`：功能变更记录。

## 已完成的重要事项

- 本地 SQLite 持久化。
- 标签、收藏夹、收藏关系持久化。
- 现有收藏可移动/复制到收藏夹。
- 现有收藏可编辑标签。
- 批量添加收藏。
- 后台排队补全详情。
- 详情页移入最近删除后跳回主页。
- Web 左侧图标栏和可折叠侧边栏。
- 我的书房移入左侧头像入口。
- 添加页支持清空残留数据。
- 完整 JSON 导入导出。
- Web SQLite localStorage 防丢备份。
- README 和项目骨架文档更新。
